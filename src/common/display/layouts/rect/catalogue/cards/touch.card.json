{
    "onPress": [
        {
            "type": "SendEvent",
            "arguments": ["cardSelected", "${data.id}"]
        }
    ],
    "item": [],
    "type": "TouchWrapper",
    "id": "${data.id}",
    "paddingBottom": "${@viewportProfile == @tvLandscapeXLarge ? 3.75vh : 6.75vh}",
    "spacing": "@spacingBetweenCards"
}
